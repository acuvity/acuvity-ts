// This file has been autogenerated using elemental cli. 
// Use `npm run sync` to update from latest elemental specifications
 
import { Identity } from '../utils/const';

{%- for model in models %}
import { {{ model.$modelName }}, {{ model.$modelName }}Identity } from './{{ model.$modelName }}';
{%- endfor %}

export type IdentityName = {% for model in models -%}
{%- if loop.index0 %}
  | {% endif %}'{{ model.$modelRestName }}'
{%- endfor %};

export const AllIdentities: Identity[] = [
{%- for model in models %}
  {{ model.$modelName }}Identity,
{%- endfor %}
];

export type IdentityNameToIdentifiable = {
{%- for model in models %}
  {{ model.$modelRestName }}: {{ model.$modelName }},
{%- endfor %}
};